import objFns from 'dummy/utils/obj-fns';
import { module, test } from 'qunit';

module('Unit | Utility | obj-fns', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = objFns();
    assert.ok(result);
  });
});
